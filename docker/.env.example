# Docker Compose settings

# Host port exposed by Docker Compose.
# The app listens on port 3218 inside the container.
HOST_PORT=3218

# Docker image used by Docker Compose.
# Defaults to ghcr.io/byte92/finance_sys:latest when unset.
# DOCKER_IMAGE=ghcr.io/byte92/finance_sys:latest
